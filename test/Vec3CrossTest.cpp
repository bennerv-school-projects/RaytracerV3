#include <cassert>
#include <memory>

#include "../src/Vector.hpp"

using namespace std;

int main(int argc, char * argv[]) {
	shared_ptr<Vec3<float> > temp0(new Vec3<float>(1, 2, 3));
	Vec3<float> temp1(1, 2, 3);
	Vec3<float> temp2(0, 0, 0);

	
}