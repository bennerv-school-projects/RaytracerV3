; Config file for settings for the ray tracer

[Settings]
anti-aliasing = true        ; anti-alias edges (4 rays / pixel)
ambient-light = 0.2         ; ambient light value
image-length = 1024          ; image length in pixels
image-height = 1024          ; image height in pixels
background-gradient = false ; background gradient drawn right away using linear interpolation
hsl-interpolation = true    ; use hsl gradients instead of rgb (will take rgb values and conver to hsl
gamma-correction = true     ; Gamma correction (color correction, disable this if using hsl-interpolation)

[Gradient]                  ; background gradient value string as defined in colors.xml
start = RED
end = GREEN